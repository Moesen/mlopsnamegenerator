from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Generating new names based on trends in data using GPT2 (Transformer network)',
    author='Moesen',
    license='MIT',
)
